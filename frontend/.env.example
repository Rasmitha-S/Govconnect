# Frontend Environment Variables (Vite)
# In unified single-domain Vercel deployment, leave empty (API requests default to relative /api)
# In split multi-project deployment, set to deployed backend API URL:
# VITE_API_URL=https://your-govconnect-backend.vercel.app
VITE_API_URL=
